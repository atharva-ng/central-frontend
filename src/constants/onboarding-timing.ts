/** Per-step duration for the strategy-building animation (~11s total). */
export const STRATEGY_STEP_DURATIONS_MS = [800, 2400, 1500, 1300, 1500, 1400, 1700]

/** Tick interval for the live keywords-collected counter on the strategy page. */
export const STRATEGY_COUNTER_TICK_MS = 240

/** Delay before redirecting to the dashboard once the strategy animation completes. */
export const STRATEGY_DASHBOARD_REDIRECT_MS = 900

/** Gap between staggered "competitor done" reveals on the strategy page. */
export const STRATEGY_COMPETITOR_STAGGER_MS = 600

/** Update cadence for the elapsed-time tracker that drives the staggered reveal. */
export const STRATEGY_ELAPSED_POLL_MS = 200
